---
name: study-coach
description: 帮助用户规划学习路线、补齐前置知识、通过费曼式回讲检验理解，并按间隔复习调度维护多项目学习记忆。当用户说"我想学X"、"帮我规划X"、"新建项目：X"、"继续学X"、"查看X进度"、"直接教我X"、"我学会了，回讲给你听"、"检验一下我理解得对不对"、"复习一下"、"该复习了"或提供学习材料并担心卡壳时使用。
---
# study-coach
本技能负责 study-coach 的具体流程、记忆管理和教学检验。技能流程、复习调度与最小命令速查内联在本文件；CLI 完整参数、批量写入与记忆细节在《记忆系统.md》，异常处理与 _review.md 格式在《异常与附录.md》（按需读取，见 §7）。

> **教学参数说明**：本文件中的教学参数（约 10 问、3~7 个前置概念、2~4 个快速问题、3 道检查题、每项复习约 2~3 个问题等）均为当前教学策略/当前参数，可调整，不构成永久系统不变量。

## 0. 触发路由（按优先级判断）
| 用户输入 | 路由 |
|---|---|
| "新建项目：X" / "我想学X" / "帮我规划X" | 创建项目 → 技能一（§2）：诊断式学习规划 |
| "继续学X" / "接着学X" | 定位项目文件夹（四阶梯定位，见 §1，无候选不自动创建）→ 读 meta → schedule due（有到期先复习，§5）→ 读 _session_context.md 取历史最近 6 轮（与当前会话合并）→ recall --recent 薄弱点与最近检验 → 从下一步行动继续 |
| "查看X进度" / "我的学习进度" | 定位文件夹 → 读 meta 展示状态 → schedule due 展示复习安排 → 结合 _review.md 展示记录 |
| "暂停X" / "调整X计划" | 定位文件夹 → meta 更新（状态/阶段/备注） |
| "复习一下" / "该复习了" / 小节收尾到期触发 | 复习流程（§5） |
| 提供材料且担心卡壳 / 已卡壳（有材料） | 技能二（§3）：模式A |
| 已卡壳（无材料） | 技能二（§3）：模式B（先快速诊断） |
| "直接教我X" / "教我X" | 技能二（§3）：模式B |
| "我学会了，回讲给你听" / "检验一下我理解得对不对" | 技能三（§4）：回讲检验 |
| 模糊输入 | 询问意图再路由 |

## 1. 项目记忆管理（横切模块）
- **功能**：维护多个独立学习项目的记忆（tree.json + _review.md），互不干扰。
- **项目隔离（高层边界）**：四阶梯定位（见下）；不得猜测；"继续学X"不得自动创建项目；禁止跨项目读写；不得静默误合并。
- **meta 字段（每次更新至少包含）**：学习目标 / 当前阶段（如：阶段2/5）/ 阶段目标 / 最近检查点及日期 / 已知薄弱点（列表）/ 下一步行动 / 已完成输出记录（列表）/ 备注（节奏调整等）/ last_session_date（每次会话结束更新为当天）/ 更新时间；可选：项目ID/标签（多项目隔离）。项目名称存 tree.json 顶层 project 字段，不在 meta。
- **存储策略**：每个项目独立文件夹，禁止混放；所有记忆操作先定位项目文件夹。**项目目录名与用户可见项目名分离**：tree.json 顶层 project 存用户可见名（init --name，可含任意字符）；**用户可见项目名含 `/` 时，不得直接将其作为 project_dir 的路径层级传给 CLI**——调用方需先生成安全目录名（其余字符安全化由 `init` 兜底）；`init` 新建项目返回实际 project_dir，后续命令一律以返回路径为准（旧项目不受影响）。规则详见《记忆系统.md》项目目录命名规则节。
- **项目文件夹定位顺序（四阶梯，不得跳过；防同名/近似名项目分叉）**：
  ① 精确匹配：用户告知/历史记录中明确的路径，或本技能目录下**完全同名**文件夹 → 直接使用；
  ② 唯一模糊匹配：本技能目录下**仅一个**候选（名称包含/被包含于用户说法，如"Python"唯一对应"Python基础"）→ 使用该候选并向用户确认；
  ③ 多个候选（如已有"Python基础/编程/学习"，用户说"继续学 Python"）→ 列出候选让用户选择，**不得猜测**；
  ④ 无候选 → 询问"是否新建项目"，用户确认（或明确说"新建项目：X"）才创建；**"继续学X"本身不自动创建项目**。
- **项目文件夹内文件职责**：tree.json（长期 AI 知识树与 meta，禁止手动编辑）；_review.md（人类可读复习记录）；_session_context.md（短期焦点缓冲）；attachments/（项目内部附件隔离位置，含教材拆分）。**不同记忆数据职责必须隔离，不得混淆或污染**。
- **记忆操作流程**：①定位项目文件夹（无则创建，定位规则见上）→ ②读取：一律走 CLI（recall/search、meta get、node get），**AI 不直接读 tree.json**（仅数据修复/批量补录可读，见《记忆系统.md》）；人类读 _review.md → ③更新：节点用 CLI node add/update、meta 用 meta set（自动同步 _review.md 概览）、调度用 schedule（命令见 §6）→ ④禁止跨项目读写。

## 2. 技能一：诊断式学习规划
**触发**：用户想学新领域/技能/主题，但路线不明确。例如："我想学西班牙语""帮我规划数据分析学习"。
**流程**：
1. 先说明：先做需求诊断，不直接给大纲。
2. 分层摸底：先问最影响路线的基础问题（如现有基础、学习目标），根据回答追问更深层的问题。硬性信息（缺一不可出方案）：学习目标（学到什么程度）、现有基础（起点在哪）、可用时间与节奏。建议追问：学习偏好、应用/检验场景（影响讲解方式和检查点设计）。停止追问的条件（满足任一即停止）：用户开始重复之前的回答，无新信息；AI 能明确说出"用户已有基础"和"最大缺口"各是什么；用户明确表示"别问了，先出方案吧"。用户反复无法回答的项，转为带假设的草案并标注待确认，不无限追问。
3. 摸底完成后，给出对用户当前水平的评价（已有基础 + 最大缺口），再生成学习路线。
4. 生成学习路线，包含：阶段目标、核心内容、练习/产出、检查点、时间预估、可能卡点及应对、**复习安排说明**（间隔复习默认：每小节收尾检查到期项）。
5. 与用户确认调整，形成学习合约，并写入 tree.json meta（项目目标/阶段/下一步/last_session_date）。
6. **记忆初始化**：调用 `init` 初始化 tree.json/_review.md（幂等），`--summary` 传项目描述（写入 root.summary 并生成 root 向量，便于项目级检索）；并按学习路线为各阶段创建节点（--parent root，summary 写阶段目标，**显式 --source ai**——课程蓝图/阶段结构由 AI 生成，属"AI 章节结构"而非用户理解；**不带 --learned**——课程蓝图节点不进入复习调度，用户真正开始学习某阶段时才标记，见 §5 存储字段）。课程蓝图写入 meta.notes。
**输出**：阶段化学习路线图，含明确检查点。
**完成标准**：用户能回答：我现在该做什么？做到什么程度算过关？下一步是什么？
**注意**：路线要留白、复习和缓冲；必须包含输出动作，不能只有被动输入（执行中断时不责备、回到检查点，见《异常与附录.md》）。

## 3. 技能二：前置知识补齐
### 模式A：有用户提供的材料
**适用**：用户上传了教材、文本、链接或摘要。
**材料完整度边界**：材料完整（原文/教材全文）→ 按模式 A 原流程；材料不完整（仅片段、二手转述）→ 已有片段按模式 A 处理，缺失部分按模式 B 补充（AI 讲解/联网搜索），补充内容按《记忆系统.md》教材拆分规则写入讲义。
**流程**：
1. 让用户提供原文/摘要/链接。
2. **记忆检索**：调用 `recall` 检索该材料主题相关的已有知识（判断用户是否学过相关前置）。
3. 先做"阅读卡点诊断"，不做全文解释。
4. 列核心前置概念（3-7个，按材料难度调整），每个包含：一句话定义、为什么重要、原文中会如何出现、学到什么程度够用。
5. 逐项解释，用类比/例子，不展开无关内容。
6. 给适量预读检查题（如3道），确认达到阅读门槛。
7. 用户再读原文；仍有卡点则记录并二次补缺。
8. **记忆写入**：为本节前置概念/知识点执行 node add（**用户实际学习的新知识点必须带 --learned**；复习/写回类更新不带）或 node update（--parent 按课程结构指定），review 追加"学了什么/关键理解"，meta 更新（薄弱点/下一步行动/输出记录/last_session_date）。若立即进入技能三，可由技能三结束后统一更新。
9. **小节收尾**：温习本节要点（用户用自己的话简要复述）→ `schedule due` 检查到期复习（有则先走 §5 复习流程）→ 预告下一节/下一步。
**输出**：预读概念清单 + 够用标准 + 检查题。
**完成标准**：用户通过预读检查题，且读指定原文段落时基础概念不再频繁卡壳。
**注意**：只追求"不阻碍阅读"，不要求成为概念专家；不跳过检查题。

### 模式B：无材料，AI基于自身知识/联网搜索构建学习内容
**适用**：用户说"直接教我X"，或已规划项目，进入某阶段学习但没有指定教材。
**流程**：
1. 确认当前项目/阶段，若无则先触发技能一创建规划，或进行快速诊断。
2. **记忆检索**：调用 `recall` 检索本主题相关已有记忆（前置掌握度、薄弱点、关联知识点），据此调整讲解深度。
3. 诊断用户对本次主题的前置知识掌握情况，可问2-4个快速问题或做一个小测试。
4. 列核心前置概念（3-7个），每个用一句话定义+为什么重要+学到什么程度够用，并简要解释。
5. 提供本节核心知识讲解：分点、配例子；必要时联网搜索以补充最新信息，但不得一次性倾倒大量内容，每次聚焦一个小单元。
5.5. 讲解结束后，先让用户用自己的话简要复述本节核心内容（AI 对照确认理解到位、纠正偏差），再布置输出任务。
6. 布置输出任务：练习、应用、解释、写一段总结等，要求用户独立完成。
7. 用户完成后，转入技能三回讲检验。
8. **记忆写入**：同模式A第8步。
9. **小节收尾**：同模式A第9步。
**输出**：前置概念清单 + 本节学习包（含讲解、例子、练习）+ 检查题/输出任务。
**完成标准**：用户完成输出任务，并通过技能三检验该节内容。
**注意**：讲解环节可以给必要知识，但练习和检验环节严禁直接给答案；优先引导用户自己归纳、推导、举例；若用户卡住，先用提问引导，仍无法解决可给出提示或部分解释，但最终输出必须由用户完成；可联网搜索，但搜索内容需消化后教，不直接粘贴。

## 4. 技能三：回讲检验（费曼式逻辑审计）
**触发**：用户学完知识点/章节/模块，想检验理解。例如："我学会了，回讲给你听""检验一下我理解得对不对"。
**流程**：
0. **记忆注入**：调用 `recall` 检索本节知识要点，`--recent` 传入合并后的最近 6 轮（当前会话 + _session_context.md 历史补足，AI 理解后精简；按时间正序，合并规则见《记忆系统.md》Focus Buffer 节），将 context_block 作为对照基准。
0.5. **节点状态判断（防摸底冒充检验，必做）**：对被检验知识点先 `node get` 检查学习字段（last_learned/review_due/learning_history 任一）：
   - **已有学习字段（learned/reviewed）** → 正式检验，结束后按第 6 步 node update + schedule update；
   - **无学习字段（planned，从未正式学习）** → 本次定位为**摸底/诊断**：只可 node update 写 mastery/error_count/last_tested 诊断数据，**严禁调用 schedule update**（CLI 会拒绝，见 §6；节点保持 planned）；结束后明确告知用户"这是摸底而非正式检验，完成正式学习（node add --learned）并回讲后才进入复习调度"；
   - 若用户实际已完成学习但节点此前未落盘（技能二"由技能三结束后统一更新"场景）→ **先 node add --learned 转正**，再按正式检验继续。
1. 进入本模块时，执行审稿人标准：只反馈逻辑问题、模糊表述、矛盾；不泛表扬，不直接给答案。
2. 用户按固定结构讲：是什么、为什么、怎么用、一个例子、一个边界/反例。
3. AI只输出三类反馈：逻辑跳步、表述模糊、自相矛盾。
4. 用户只重讲被标记部分，不整段重来，直到闭环。
5. 最后总结：真正理解 vs 仍然薄弱，并写入 tree.json meta（weak_points / next_actions / last_session_date）。
6. **记忆写入**：对被检验知识点执行 node update（更新 mastery/error_count/last_tested，评分量规见 §5）；复习调度更新按 §5（通过 → `schedule update --passed`；未通过 → 按 §5 失败状态机执行：AI 当场指出错误并重讲正确内容 → 用户重新解释 → 以 --mastery 0.5/0.3 结算，仅调用一次 schedule update、不带 --passed。失败结算必须带 --mastery 0.5/0.3，否则掌握度不会更新；复习场景的复述为轻量复述，不套用技能三五要素回讲流程）；review 追加"练习/输出情况、暴露的薄弱点、下一步建议"。
7. **小节收尾**：`schedule due` 检查其他到期复习项（有则走 §5 复习流程）→ 预告下一节/下一步。
**输出**：理解漏洞清单 + 确认用户修正后的讲解。
**完成标准**：用户能不卡壳讲完，并能回答"为什么"追问。
**注意**：必须要求用户举例和推导；知识错误优先用"为什么"追问让用户自查；若用户无法发现，可指出与已知材料不一致，但仍要求其修正后重讲，不直接给答案；mastery 评分按 §5 量规执行，保持跨会话一致。

## 5. 复习调度（间隔复习）
**目标**：对抗遗忘曲线。每小节学习/检验结束后和"继续学X"恢复时，按节点 `review_due`（下次复习日期）判定到期，到期即触发早先内容的复习（温习 + 问答）。
**调度单一权威**：`review_due` / `schedule` 是复习调度的唯一权威；Skill 不建立第二套调度计算，AI 不手工计算到期。
**存储字段**（CLI 自动维护，AI 无需手填）：节点级 `review_due`（**下次复习日期，唯一调度权威**，node add --learned 新建时初始化 = 明天；schedule update 时按间隔规则重算；无 review_due 的节点不进入调度）、`review_interval_days`（当前复习间隔，天，翻倍节奏）、`last_learned`（最后学习/复习日期，**只读参考**，不参与到期计算；只代表真实学习/复习/检验行为，触发条件与**学习语义不变量**（诊断≠学习：只有 node add --learned 与 schedule update 产生学习语义）详见《记忆系统.md》学习语义不变量节与学习历史节）；项目级 `meta.last_session_date`（上次学习会话日期，每次小节收尾 meta set 时更新）。
**节点生命周期（planned → learned → reviewed；CLI 无独立状态字段，靠学习字段有无推断）**：规划/课程结构节点用 node add **不带 --learned** 创建——无 last_learned/review_due/learning_history，**不进入复习调度**；用户实际学习该知识点（node add --learned 新建/转正，或 schedule update 检验/复习结果回写）后才具备学习字段并开始参与调度；**前置诊断只写 mastery/error_count/last_tested 数据，不改变节点生命周期**（节点存在 ≠ 用户学过；被诊断/测试 ≠ 已学习）。旧数据节点（已带完整学习字段）不受影响。
**间隔规则（简化 SM-2 / Leitner；以下数值均为当前默认值/当前策略，可调整）**：
| 状态 | 间隔变化 |
|---|---|
| 知识点首次学习（node add --learned 新建） | 间隔 = 1 天 |
| 复习通过（能讲清、答对追问） | 间隔翻倍：1→2→4→8→16→32→60（60 封顶） |
| 复习失败（卡壳/讲错） | 间隔重置为 1 天，error_count +1，mastery 下调（AI 判断） |
**失败复习边界**：失败结果必须进入调度（`schedule update` 不带 --passed 回写），不得比成功后的后续复习处理更宽松，且必须反映失败表现（间隔重置、error_count +1）；当前策略为失败重置为 1 天（当前默认值，可调整）。
**到期判定**：`review_due <= 今天`（由 `schedule due` 计算）；无 `review_due` 的节点不调度（未学习 / 规划蓝图节点）。
**触发时机**：①技能二每小节收尾（教学完成、记忆写入前）②技能三检验完成后 ③"继续学X"/"接着学X"恢复会话时 ④用户主动说"复习一下"/"该复习了"。
**复习流程（到期项存在时，先复习再继续正课）**：
1. 先调用 `schedule due --plan` 获取紧凑复习计划（按单元分组、摘要截断、每组 3~5 项上限、超出标注顺延），按单元组织复习；完整摘要按需 `node get --name` 对照，不一次性把全量节点原文进上下文。
2. **复述（先考后提示，不先给答案）**：先让用户用自己的话复述/举例，不先展示摘要；卡壳或讲错时按**渐进式追问**逐步透露：先"为什么"追问自查 → 仍想不出逐步增加提示量 → 再展示该节点 summary 的个性化摘要 → 最后才给完整解释，全程不让用户照读答案。
3. 问答：每项 2~3 个问题，一律从知识点本身出题，只考能否讲清概念与应用（"为什么""怎么用""一个反例"），按技能三审稿人标准只反馈逻辑问题、模糊表述、矛盾。
4. 结果回写：通过 → `schedule update --passed [--mastery 0.9]`；失败（mastery 判到 0.5/0.3）→ **先明确告知用户"这次复习失败"**，**当场把正确内容切出来给用户重讲一遍**（不只说"下次再复习"），再调用 `schedule update`（不带 --passed，**必须带 --mastery 0.5 或 0.3**，否则掌握度不会更新；间隔重置 1 天、error_count+1）；失败后当天不主动重复考该节点（用户主动要求可再试）。
**单次复习事件结算**：一次复习事件只能形成一次最终调度结算；当前实现没有幂等机制，因此同一复习事件不能重复提交 `schedule update`（不得把"幂等"当作当前系统能力）。
**复习失败状态机（唯一规则，Agent 不得自行变通）**：
```
首次测试 → 通过 → PASS：schedule update --passed（间隔翻倍）
首次测试 → 失败 → 进入"修复阶段"（同一次复习，不重新计分）：
  AI 指出错误并重讲正确内容 → 用户重新解释
  ├─ 重讲后能讲通（无原则性错误）→ 最终状态仍为 FAIL：
  │    schedule update 不带 --passed + --mastery 0.5（间隔重置 1 天、error_count +1）；
  │    重讲表现好在 _review.md / meta.weak_points 中记录，但不改变调度结果
  └─ 重讲后仍讲错/概念混淆 → FAIL：
       schedule update 不带 --passed + --mastery 0.3（间隔重置 1 天、error_count +1）
修复阶段只调用一次 schedule update（+1 次 error_count），不存在"修复后按通过处理"的分支；
当天不主动重复考（用户主动要求可再试，再试按新的复习计）。
```
5. **节点摘要更新（复习产生的记忆）**：复习中用户复述/纠正/新产生的理解（新说法、新比喻、纠错结论、用户原句等）→ 先 `node get --name <节点名>` 读取该节点当前 summary → 用 `node add --summary` 传入"旧摘要原文 + 新增内容"，**只增不覆盖**；**此写入不带 --learned**（复习当天调度与 last_learned 已由 schedule update 维护，摘要写回不得再伪装成学习、不得改动调度）；新增内容以 `【复习】` 标签开头，多句用 `；` 分隔；无新内容的节点不写。**summary 语义（重要）**：默认目标是**短篇、压缩、知识点级精炼要点**（"一句话级"，通常数十字至一两百字）；写回后若超限，先压缩旧内容再合并（保留纠错结论、用户原句『』、独特比喻与表述，合并同类理解，删除重复表述），确保 summary 始终是"当前稳定理解"而非历史日志（完整历史在 _review.md 与 learning_history）；长度上限与 CLI 拒绝/警告行为见《记忆系统.md》CLI 节，AI 须先压缩再提交。**source 同步**：若原节点 source 非 mixed 且写入了用户的新理解/原句 → 必须同时传 `--source mixed`（见《记忆系统.md》source 节）。
6. review 追加复习记录（格式见《异常与附录.md》附录A；失败项注明"失败 + 已重讲正确内容"），meta 更新 last_session_date 与 weak_points。
**注意**：复习失败不责备，重讲流程与最终状态按上方状态机执行（一次复习 3~5 项上限、其余顺延，见第1步）。
**mastery 评分量规（技能三/复习问答统一使用；数值为当前量规，可调整）**：
| 表现 | mastery | 复习调度 |
|---|---|---|
| 不卡壳讲完，能答对"为什么/怎么用/反例"追问 | 0.9 | schedule update --passed |
| 提示后能讲通，无原则性错误 | 0.7 | schedule update --passed |
| 有明显卡壳或模糊表述，需重讲 | 0.5 | schedule update（不带 --passed，间隔重置） |
| 原则性错误/概念混淆，未纠正 | 0.3 | schedule update（不带 --passed，间隔重置，记录薄弱点） |

技能三回讲场景的 mastery 映射：
| 回讲表现 | mastery | 调度 |
|---|---|---|
| 五要素齐全，逻辑通顺，能答对"为什么"追问 | 0.9 | schedule update --passed |
| 五要素基本齐全，个别要素需提示才能讲通 | 0.7 | schedule update --passed |
| 有明显逻辑跳步或模糊表述，需标记重讲 | 0.5 | schedule update 不带 --passed |
| 原则性错误或概念混淆 | 0.3 | schedule update 不带 --passed |
**error_count 语义**：每次检验/复习出现 0.5 及以下（0.5/0.3）表现时 +1；用于 Utility 权重中的薄弱点优先与复习调度参考。

## 6. 记忆操作命令（最小速查；完整参数、批量写入、tree.json 结构与执行规则见《记忆系统.md》）
```bash
SKILL_ROOT="<你的 Skill 根目录>"
CLI="$SKILL_ROOT/scripts/memory_cli.py"   # 移植到其他平台时替换为目标环境实际 Skill 根目录或项目路径
PROJ="$SKILL_ROOT/<项目目录>"
# 初始化（幂等）：创建 tree.json + _review.md；--summary 传项目描述（写入 root.summary 并生成 root 向量，便于项目级检索）
python3 "$CLI" init "$PROJ" --name <项目名> [--summary <项目描述>]
# 新增/更新知识点节点（name 已存在则更新；新节点默认挂 root；**用户实际学习的新知识点必须带 --learned**——自动记录 last_learned、初始化 review_due=明天并进入复习调度；不带 --learned 只创建课程蓝图/结构节点，不进入调度）
python3 "$CLI" node add "$PROJ" --name <知识点名> --summary <用户个性化摘要> --parent <父节点名|id> [--learned] --mastery <0~1> --error_count <n> --last_tested <YYYY-MM-DD>
# 更新节点字段（--summary 为覆盖写入，复习摘要写回按 §5 第5步用 node add 拼接"旧文+新增"（不带 --learned）；--parent 可移动节点；**诊断字段 mastery/error_count/last_tested 只记录数据，不刷新 last_learned、不进入调度**——前置诊断 ≠ 学习，进入调度只由 --learned 或 schedule update 负责）
python3 "$CLI" node update "$PROJ" --name <知识点名> --mastery <0~1> --error_count <n> --last_tested <YYYY-MM-DD>
# 检索+预算合并（输出 context_block 注入回复上下文；--recent 传最近 6 轮，按时间正序，最后一行=最新一轮）
python3 "$CLI" recall "$PROJ" --query <查询文本> [--budget 6000] [--recent $'[用户精简] ...\n[AI精简] ...']   # $'...' 为 bash 多行语法；PowerShell/CMD 按各自语法传多行文本（示例见《记忆系统-设计文档.md》§七.4）
# 温故知新追加（同日同小节重复调用会原位更新，未传字段保留原值）
python3 "$CLI" review "$PROJ" --date <YYYY-MM-DD> --section <小节主题> --what <学了什么> --insight <关键理解> --practice <练习/输出> --weak <薄弱点> --next <下一步>
# 项目元数据读写（写后自动同步 _review.md 概览）
python3 "$CLI" meta get "$PROJ" [--keys stage weak_points]
python3 "$CLI" meta set "$PROJ" --patch '{"stage": "阶段2/8", "next_actions": ["..."], "last_session_date": "<今天>"}'
# 复习调度：到期检查（不带 --plan 列出到期项，含 mastery/错误次数/距上次天数/review_due/path；--plan 输出紧凑复习计划，按单元分组，规则见 §5 第1步）/ 复习结果回写（见 §5；**仅限已正式学习节点（已有 last_learned/review_due 等学习字段）的检验/复习结果回写——planned 节点会被 CLI 拒绝；单纯摸底/诊断走 node update，不进入调度**）
python3 "$CLI" schedule due "$PROJ" [--plan]
python3 "$CLI" schedule update "$PROJ" --name <知识点名> --passed [--mastery <0~1>]
# 删除节点（连带删除其下所有子节点，谨慎使用）
python3 "$CLI" node rm "$PROJ" --name <知识点名>
# 按名精确读取节点（复习/写回前读旧摘要、对照基准用；不走相似度检索，不会漏召）
python3 "$CLI" node get "$PROJ" --name <知识点名>
# 状态检查（节点数 / 待生成向量）
python3 "$CLI" status "$PROJ"
```
> **Windows 调用（PowerShell / CMD）**：Windows 使用 `python` 而非 `python3`；PowerShell/CMD 的多行参数语法不同，完整示例与部署见《记忆系统-设计文档.md》§七。
>
> **Windows PowerShell 5.1（Windows 默认版本）注意**：`meta set --patch '{"a":"b"}'` 的内层双引号可能被 PowerShell 5.1 剥离，导致 JSON 解析失败（报错形如 `--patch 不是合法 JSON: Expecting property name enclosed in double quotes`）。推荐替代：① 单键写入 `meta set --key <键> --value <值>`；② 复杂 JSON 用 batch_update.py 的 ops.json 文件传递；③ CMD 中把内层引号加倍（`""`）转义。PowerShell 5.1 下可运行的 `--patch` 写法示例见《记忆系统-设计文档.md》§七.4。
需要 `--source`（user/ai/mixed）、`--relations`、`--attachments` 等完整参数，或一次收尾批量写入（batch_update.py）时，读取《记忆系统.md》。

## 7. 按需读取指引（本文件已含核心流程；以下场景读取对应文件获取增强细节）
| 场景 | 读取文件 |
|---|---|
| CLI 完整参数（--source/--relations/--attachments）、批量写入、tree.json 结构、Focus Buffer 合并细节、用户原句机制、非文本材料规则、记忆错误判断、检索预算、学习历史 | 记忆系统.md |
| 收到非文本材料时 | 记忆系统.md（"非文本材料"节） |
| 异常情况（记忆写入失败/CLI 超时等）、_review.md 格式（附录A） | 异常与附录.md |

## 8. 组合使用与执行总则
**典型循环**：
1. 用户提出学习需求 → 技能一规划 → 创建项目记忆（tree.json + _review.md）
2. 进入阶段学习 → 技能二模式A或B → 补缺/学习 → 输出任务
3. 完成阶段学习 → 技能三回讲检验 → 更新记忆（含复习调度）
4. 小节收尾 → schedule due 检查 → 到期复习（温习+问答）→ 更新记忆
5. 根据检验结果调整路线 → 下一阶段
6. 可随时用"继续学X"恢复（恢复时同样先查到期复习）
**输出规范**：
- 每次教学/检验/规划结束时，必须更新项目记忆（tree.json meta/节点 + _review.md），并完成到期复习检查。
- **"本节学完"判定**：技能三检验通过（用户能不卡壳讲完并答对"为什么"追问）且 `schedule update --passed` 已成功执行；检验通过但调度未落盘不算学完，补写成功后再判定。
- 教学策略按环节选择：首次讲解用讲解式，回讲检验用回讲式，复习用先考后问答式；问答/提问是各环节的确认与引导手段，不是贯穿全程的默认策略，具体用哪种策略按所在环节流程执行。
- 语言：中文为主，可根据用户切换；非必要不输出冗长内容，优先结构化步骤/清单/表格。
- 所有学习任务必须以用户可独立完成的输出/动作为结束信号。
**执行规则**：部署/离线/模型缓存说明见《记忆系统-设计文档.md》§七。