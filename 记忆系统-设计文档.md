# study-coach 记忆系统设计文档
> 依据《系统实现规范：study-coach 记忆系统》落地，参考 HCE（Holographic Context Engine, github.com/nakurian/hce）三大结构设计。

## 一、架构落地映射

| HCE 结构 | 本系统落地 | 说明 |
|---|---|---|
| Semantic Tree | `tree.json` 知识树 | 嵌套对象，root id 固定为 `root`，每个节点含 id/name/summary/vector/children，可扩展 mastery/error_count/last_tested/relations |
| Entity Graph | tree.json 节点扩展字段 | 不另建图文件；诊断数据（掌握度/错误次数/薄弱点/诊断历史）与横向关联（relations）直接挂载节点上 |
| Focus Buffer | 跨会话焦点缓冲 | 落盘为 `_session_context.md`（AI 直接读写，CLI 不涉及）；AI 从当前会话提取最近 6 轮精简写入，跨会话读取时与历史合并后传入 `recall --recent` |
| HRS | `hr_search()` | numpy 暴力余弦 + 分层剪枝（按当前层相似度排序后优先深入）+ 访问上限限制递归深入（避免无界遍历） |
| Context Budgeting | `knapsack()` | 贪心背包，按 Utility/Token_Cost 装入，默认预算 6000 token |

## 二、已确认决策（用户选定）

1. **嵌入模型：BAAI/bge-small-zh-v1.5**（100MB / 512 维 / 24M 参数）
   - 实测（aarch64 CPU）：首次加载 43s（含下载），缓存后加载 9.1s，单条编码 0.079s
   - 依据：系统语言以中文为主（SKILL.md、项目内容、用户对话均为中文，仅术语不做语言要求）
    - 查询侧使用 bge 官方指令前缀"为这个句子生成表示以用于检索相关文章："（仅用户查询，doc 侧不加）
   2. **焦点缓冲：AI 理解后精简，N=6 轮，跨会话落盘 `_session_context.md`**（机制：AI 从当前会话提取最近 6 轮精简写入、跨会话读取时与历史合并后传入 `recall --recent`；操作规则见《记忆系统.md》"Focus Buffer"节）
   - 设计依据：N=6 覆盖典型"教学→练习→回讲"闭环；符合人类记忆"理解后重构"的本质（Bartlett 重构记忆），而非录像式存储；跨会话落盘使"继续学X"能衔接上次对话上下文

## 三、自主设计项与默认值（含调整依据）

### 1. 相似度阈值：默认 0.45
- bge 语义空间余弦值分布与 HCE 原版 hash 向量（0.1）不同，实测中文知识点相关检索 sim 多在 0.45~0.75，无关约 0.3 以下
- **防无界递归深入双保护**：
  - 单层通过率 >70% 且阈值 <0.50 时，阈值上浮至 0.50（`THRESHOLD_FLOOR`）。**上浮只收紧子层深入**（后续层的 passed 筛选与深入用 0.50），本层已通过节点保留——防边缘误杀（实测 sim=0.47 的节点曾被 0.50 阈值误剪，如"白色"复习漏召事故）
   - 全局访问节点上限 = max(10, min(总节点数×25%, 100))，超限停止深入。**该上限约束"进入结果并继续深入的节点数"（visited 计数：通过阈值且被 walk 的节点）**；单层 child_sims 与旁路 sub_sims 的批量 cosine 评估在深入之前完成、不计入预算——防的是"无限深入/全量递归"，不严格限制单层扇出的相似度计算量（宽树场景批量计算可能超出上限，实际项目树规模为几十~数百节点，开销可接受；如需严格约束计算量需重写 HRS 预算模型，当前不做）
- 调整依据：若检索召回过少（hits 空），可下调至 0.40；过多（单层通过率频繁触发上浮）可上调

### 2. Utility 公式
```
U = 0.50×sim + 0.20×(1-mastery) + 0.15×min(error_count,5)/5 + 0.15×time_decay
time_decay = 1 - exp(-天数/30)   # last_tested 距今越久 decay 越大（30 天尺度）
            0.6                  # 从未记录 last_tested（≈27 天前水平，照顾新节点）
```
- 设计意图：相关性为主（0.50）；**mastery 项采用"弱项得分"语义（weakness = 1 - mastery）**，掌握度越低 Utility 越高，与 error_count（0.15）共同实现"薄弱点优先被召回"（合计 0.35）；时间衰减防止久未复习的知识被遗忘
- 修正说明：旧公式为 `+0.20×mastery`（正向），数学上导致"掌握越好越优先注入"，与薄弱点优先目标直接冲突（0.20 正向与 0.15 反向互相抵消后仍偏向高掌握）；已改为弱项得分语义，并在 memory_cli.py `node_utility()` 同步

### 3. Token 估算
```
tokens = 中文字符数×1.0 + 英文/数字词数×1.3（下限 1）
```

### 4. 节点父子关系判断规则（实测标定）
- **显式 --parent 优先**：AI 依据课程/教材结构指定（如"色相"→"色彩三要素"）
- 未指定时自动判断（仅防重复合并，**不自动挂子节点**）：
  - 相似度 ≥0.85（MERGE_SIM）→ 视为同一知识点，合并更新
  - 否则挂 root
- 标定依据（实测 doc-doc 相似度）：
  - "色彩三要素 vs 饱和度"=0.766（父子关系却会被 0.75 误合并 → 合并阈值提至 0.85）
  - "色相 vs 白平衡"=0.546（同主题不同概念，会被 0.45 误挂子节点 → 取消自动挂子）
  - "饱和度 vs 自然饱和度"=0.857（同知识点，0.85 合并正确）
- 同名节点重复 add 自动按更新处理（不创建重复节点）

### 5. 向量生成失败降级
- 节点向量生成失败 → vector=[] + `vector_pending` 标记 + 顶层 `pending_vectors` 列表登记
- 检索时：空向量节点**跳过自身（不匹配、不进结果），但继续深入其子层**——防止中间节点向量失败导致其整棵子树不可召回（HRS 不中断；旁路直查机制保证其有向量的后代仍可达）
- 下次 node add/update 时自动重试生成；status 命令可查看 pending 列表
- root 向量为空时不影响检索（root 处理见 6）

### 6. 检索时的 root 处理
- root 是项目容器（向量=项目描述），query 与其相似度通常不高
- **root 不参与剪枝**（始终放行作为入口）、**不加入检索结果**（避免"项目名"混入注入上下文）

### 7. Focus Buffer 与 tree.json 合并预算策略
- 统一候选池：树检索结果（HRS 全部通过节点）+ 缓冲条目（recency utility = 0.5+0.5×位置占比）
- 6000 token 贪心装入、超限淘汰旧轮次不截断、最近 1 轮强制保留等规则见《记忆系统.md》Focus Buffer 节，此处不重复

### 8. 项目文件夹创建时机
- 完全沿用 SKILL.md 触发路由：新建项目/继续学/查看进度时按需创建文件夹；`init` 幂等（已存在 tree.json/_review.md 时不覆盖），`init --summary` 用法见 SKILL.md §2 第6步（写入 root.summary 并生成 root 向量；此前无 CLI 写入路径）

### 9. 项目元数据 meta（存于 tree.json 顶层）
- 字段清单见 SKILL.md §1（唯一规范位置），此处不重复
- 读写：`meta get/set` 子命令（set 支持 `--patch` JSON 对象批量合并或 `--key/--value` 单键），写后**自动同步更新 _review.md 头部项目概览**；触发时机见《记忆系统.md》"记忆写入时机"表
- 依据：规范约束1允许"tree.json 可扩展额外元数据字段"；项目级信息由 meta 承接，保证 AI 跨会话恢复能力不丢失

### 10. 复习调度（间隔复习）
- 字段定义（review_due/review_interval_days/last_learned/meta.last_session_date）、间隔规则（翻倍 1→2→4→8→16→32→60、失败重置 1 天）、到期判定、触发时机、失败处理流程与 last_learned 触发条件，见 SKILL.md §5（命令速查见 §6），字段维护细节见《记忆系统.md》"学习历史"节，此处不重复
- **review_due 为唯一调度权威**：用绝对日期替代"last_learned + interval 差值计算"，到期判定只看标签（`review_due <= 今天`）；`last_learned` 降级为只读参考（只代表真实学习/复习/检验行为，被诊断/测试 ≠ 已学习），避免历史数据日期差累积误差
- **简化复习模型而非 SM-2**：不做精确的 SM-2 质量分级（E-factor），用"通过/失败"二元 + mastery 量规（见 SKILL.md §5），保持 CLI 简单；间隔上限 60 天防无限拉长；复习内容压缩仅靠脚本结构化逻辑（bge 无生成能力，不做 AI 摘要）
- **不单独设状态字段（planned/learned/reviewed）**：节点生命周期靠学习字段（last_learned/review_due/learning_history）有无推断，不建独立状态字段——避免状态与学习字段双份维护不一致；旧数据节点（已带学习字段）天然兼容。具体生命周期规则见 SKILL.md §5
- **schedule update 生命周期保护（设计取舍）**：这是**双保险**——流程上技能三第0.5步先 node get 判断状态，代码上 schedule update 强制校验，防止"摸底/诊断冒充正式检验"把 planned 节点错误推进为 learned（first_learned 被误设、错误进入复习调度）。运行规则（仅限已正式学习节点、planned 节点被拒绝等）见《记忆系统.md》"学习语义不变量"节；旧数据补录场景（有 last_learned 的已学习节点）不受影响
- **与现有机制的关系**：time_decay 以 last_tested 为参考（未检验过的新节点取中性值 0.6）；复习/检验通过会更新 last_tested，使 time_decay 归零（该项得分最低），符合"刚复习过的知识不优先注入"的直觉——刚复习过的记忆新鲜、无需优先带入上下文，久未复习的 decay 大、优先注入。review_due / last_learned / learning_history 均不参与 Utility 计算

### 11. 用户原句机制（原"金句机制"）
- 判断标准（三问）、期望锚点、存储与引用规则等操作细节见《记忆系统.md》"用户原句机制"节，此处不重复
- **设计取舍**：不做独立的 quotes 原文层/need_verify 复核（摘录片段本身也可能断章取义，用偏差防偏差增益有限），纠错靠回讲矛盾 + 用户反馈

### 12. 节点来源 source（user/ai/mixed）与教材拆分
- source 定义、状态转移规则与写入方式（默认 user、仅显式传入时更新、mixed 需拼接重传）见《记忆系统.md》"节点来源 source 字段"节，此处不重复
- **设计取舍（source 为什么存在）**：区分"用户自己的理解/原句"与"AI 教学/解析"两类记忆来源，支撑检索按来源过滤（--source），并让节点内容构成如实可查（user 与 AI 内容并存即 mixed），不依赖 AI 现场猜断
- **tree.json 与教材拆分讲义的分工**：不另建独立解析文件——AI 解析直接进 tree.json 节点（source=ai），单元级详细解析进 `attachments/materials/<教材名>-<大纲单元名>.md` 拆分讲义。**节点管知识点级记忆（精炼要点），讲义管单元级教学素材**；summary 长度约束与拆分操作规则（时机/命名/内容/原始教材保留）见《记忆系统.md》"非文本材料"节，此处不重复

### 13. 学习历史（learning_history / first_learned）
- 字段定义与维护规则（10 条上限、带 --learned 的 add/转正与复习追加、普通修改不追加）见《记忆系统.md》"学习历史"节，此处不重复
- **设计目的**：记录节点"何时开始学、学过几次"的学习轨迹，供 AI 按需引用（如"你去年冬天学……"）；**不参与复习调度**（到期判定只认 `review_due`，避免双权威）与 **Utility 计算**（掌握度由 mastery/error_count/last_tested 表达，学习时长不直接反映当前水平）；检索结果返回但不注入 context_block（避免挤占 6000 token 预算）

## 四、环境部署

| 项 | 值 |
|---|---|
| 运行环境 | proot Ubuntu（实测环境；脚本纯 Python，跨平台可移植） |
| 依赖 | Python ≥3.10、numpy≥1.24、sentence-transformers≥2.3（**最低兼容版本**；torch/transformers/huggingface_hub 由 sentence-transformers 依赖带入，不单独锁版本。实测 3.12.3 / numpy 2.5.2 / torch 2.13.0 / sbert 6.0.0——仅记录**已验证环境**，不作硬性平台要求）。安装命令见 §七.2 |
| 模型 | BAAI/bge-small-zh-v1.5；缓存位置由 Hugging Face 默认机制或环境变量决定（**缓存位置 ≠ Skill 安装位置**），部署细节见 §七 |
| 网络注意 | 系统环境固化了失效的 `HF_ENDPOINT=https://hf-mirror.com`（该镜像现仅 308 重定向）；memory_cli.py 默认官方源 `https://huggingface.co`，但**尊重用户已有配置**（`os.environ.setdefault`）——用户显式设置 `HF_ENDPOINT`（如配置可用镜像）时以用户为准，不会被无条件覆盖 |
| 模型下载 | 首次使用从 huggingface.co 下载（约 100MB，之后离线可用；流程见 §七.5）；若官方源未来不可达，需重配镜像后再下载一次（已缓存则不受影响） |

## 五、文件清单

> 目录结构统一使用抽象占位符：`<SkillRoot>` 表示当前 Agent / Harness 实际挂载本 Skill 的根目录（位置由当前运行环境决定，不是固定安装路径）；`<项目目录>` 表示 SkillRoot 下的某个学习项目目录。不要用某个具体平台（C:\ / D:\ / /sdcard/...）的安装路径替代这里的通用结构。

```
<SkillRoot>/                        # 当前 Agent / Harness 实际挂载本 Skill 的根目录
├── SKILL.md                     # 核心常驻：路由 + 技能流程 + 复习调度 + 命令速查
├── 记忆系统.md                  # 记忆细节手册（CLI 全量/批量/Focus Buffer/非文本等，按需读）
├── 异常与附录.md                # 异常边界 + _review.md 格式（按需读）
├── 记忆系统-设计文档.md          # 本文档（设计依据/取舍/部署指南）
├── scripts/
│   ├── memory_cli.py            # 记忆系统 CLI（运行时：init/node/search/recall/review/meta/schedule/status）
│   └── batch_update.py          # 批量写入（运行时：单进程复用嵌入模型）
└── <项目目录>/
    ├── tree.json                # AI 知识树 + meta（节点含 learning_history / first_learned / source）
    ├── _review.md               # 人类可读温故知新
    ├── _session_context.md      # 短期焦点缓冲（最近一次会话最后 6 轮，AI 直接读写）
    └── attachments/             # 非文本附件（原文件隔离副本）
        └── materials/           # 教材拆分讲义（按需创建，无教材不创建）
```
> <项目目录> 沿用原 SkillRoot 下的数据，不复制、不新建，避免同一项目出现两份 tree.json 导致记忆分叉（定位顺序见 SKILL.md §1）。

## 六、已知边界与后续调整

1. **模型加载 9s/次**：每次 CLI 调用加载一次模型。当前写入频率低（小节结束才写），可接受；若后续需要高频调用，可升级为常驻服务（serve 模式）或 ONNX Runtime 推理（加载约 1~2s）
2. **阈值与 Utility 权重**：随实际使用数据积累可再标定（记录调整前后对比即可）
3. **缓冲跨会话**：机制见二.2与《记忆系统.md》Focus Buffer节；完整历史恢复仍依赖 tree.json（meta 项目状态 + 节点知识/诊断）
4. **并发**：单用户场景，原子写保证单进程不损坏文件；多进程并发写入未加锁（规范允许暂不处理）。当前平台工具调用为**串行**（单 Agent 顺序执行 CLI），batch_update 与 memory_cli 为进程内复用不会并行，暂无真实并发路径；若未来出现多 Agent/多进程并发写同一项目（last-writer-wins 丢更新而非文件损坏），需引入跨平台文件锁（暂不实现）
5. **复习调度**：间隔参数（初始/上限/翻倍节奏）可随使用数据调整，改动仅涉及常量。旧数据节点无 `review_due` 时 schedule due 不列出（2026-08-24 已对"学习调色"完成迁移：已学习节点按 last_learned + interval 推导补录、未学习节点不设；新节点由 node add --learned 初始化，不带 --learned 创建的结构节点不进入调度）；若出现无 review_due 的已学习节点，可用 node add --learned 同名更新或 schedule update 补录（node update 只写诊断数据，不再初始化调度）
6. **SkillRoot 与项目数据同目录（边界说明，当前不重构）**：项目数据位于 SkillRoot 下是既有设计——本环境（单用户本地目录）Skill 目录可写、无自动覆盖更新机制、四阶梯定位规则已防同名分叉（SKILL.md §1），不构成实际架构问题。若未来宿主将 Skill 目录**只读挂载**或**自动覆盖更新**（如包管理器安装），需引入 DATA_ROOT（环境变量或约定路径）把项目数据解耦到 Skill 本体之外，并自动兼容旧 `SkillRoot/<项目目录>` 定位——届时再实施，当前不做代码改动

## 七、从零部署指南（移植到其他 agent / 平台）

适用：把本技能迁移到另一台设备或另一 agent 平台（Windows / WSL / macOS / 其他 Android 环境）。

迁移分为两套，不要混在一起：

- **Skill 数据（可迁移）**：SKILL.md、记忆系统.md、记忆系统-设计文档.md、异常与附录.md、scripts/、<项目目录>（tree.json / _review.md / _session_context.md / attachments/ / materials/）；
- **目标运行环境（在目标平台重新部署，不随 Skill 迁移）**：Python、numpy、torch、sentence-transformers、模型缓存。**不要把源平台（如 Android ARM64）的 Python / torch / 模型运行环境复制到目标平台**。

标准迁移 = Skill 数据迁移 + 目标平台重新安装运行环境（首次运行自动下载模型，见 §七.5）。

### 1. 环境要求

- Python ≥ 3.10（依赖明细见第四节）
- 可联网安装依赖；首次使用需联网下载嵌入模型（约 100MB，之后离线可用）

### 2. 安装依赖

```bash
pip install "numpy>=1.24" "sentence-transformers>=2.3"     # 最低兼容版本见第四节（numpy / sentence-transformers）
# 若 torch 默认源安装失败（网络受限），可先装 CPU 版：
pip install torch --index-url https://download.pytorch.org/whl/cpu
```

### 3. 准备技能目录

将 Skill 数据（SKILL.md 及各功能文件、scripts/、各项目文件夹）复制到目标位置 `<你的 Skill 根目录>`（即目标平台的 SkillRoot）。`<你的 Skill 根目录>` **不是固定路径**，由当前 Agent / Harness / 用户实际确定；仅作示意时可写 `D:\skills\...`、`~/skills/...` 等平台示例，不要写死为通用安装位置。

### 4. 路径参数化

SKILL.md 中 CLI 路径为**示例**，使用时替换为本机实际路径（Windows 下用 `python` 而非 `python3`，路径含中文建议加引号）：

```bash
SKILL_ROOT="<你的 Skill 根目录>"
CLI="$SKILL_ROOT/scripts/memory_cli.py"
PROJ="$SKILL_ROOT/<项目目录>"
# Android/Termux 示例：/sdcard/<你的目录>/skills/study-coach/scripts/memory_cli.py
# Windows 示例（仅为平台示例，勿写死为通用安装位置）：D:/skills/study-coach/scripts/memory_cli.py
```

Windows PowerShell（`<你的 Skill 根目录>` 由当前 Agent / Harness / 用户实际确定，不要写死安装位置）：

```powershell
$SKILL_ROOT = "<你的 Skill 根目录>"
$CLI = Join-Path $SKILL_ROOT "scripts\memory_cli.py"
$PROJ = Join-Path $SKILL_ROOT "<项目目录>"
python $CLI status $PROJ
```

Windows CMD：

```cmd
set "SKILL_ROOT=<你的 Skill 根目录>"
set "CLI=%SKILL_ROOT%\scripts\memory_cli.py"
set "PROJ=%SKILL_ROOT%\<项目目录>"
python "%CLI%" status "%PROJ%"
```

Windows PowerShell 传多行 `--recent`（here-string，替代 bash 的 `$'...'`）：

```powershell
$RECENT = @"
[用户精简] 上一轮用户说了什么
[AI精简] AI 的回复要点
"@
python $CLI recall $PROJ --query "查询文本" --recent $RECENT
```

**Windows PowerShell 5.1 注意（`meta set --patch`）**：PowerShell 5.1（Windows 默认版本）向原生程序传参时会剥离参数内层的双引号，`--patch '{"stage": "阶段2/8"}'` 传给脚本的实际是 `{stage: 阶段2/8}`，导致 JSON 解析失败（报错形如 `--patch 不是合法 JSON: Expecting property name enclosed in double quotes`）。替代方案（按场景选择）：

- 单键写入（简单场景）：`python $CLI meta set $PROJ --key "stage" --value "阶段2/8"`
- 复杂 JSON（批量）：把操作写入 ops.json 后由 batch_update.py 执行（JSON 在文件内，不经命令行传递，无引号剥离问题）
- CMD 转义：CMD 中把内层双引号加倍（`""`）即可正常传递，如 `--patch "{\"\"stage\"\": \"\"阶段2/8\"\"}"`

若必须在 PowerShell 5.1 中直接使用 `--patch`，以下两种写法均实测可运行：

```powershell
# 方式1：反引号转义内层双引号（推荐）
python $CLI meta set $PROJ --patch "{`"stage`": `"阶段2/8`", `"next_actions`": [`"学语法`"]}"

# 方式2：单引号内双引号加倍
python $CLI meta set $PROJ --patch '{\"\"stage\"\": \"\"阶段2/8\"\"}'
```

**CMD 多行限制**：CMD 无法直接在参数内包含换行符——引号内的换行会被当作命令分隔符，导致参数截断或报"不是内部或外部命令"。若需传递多行文本（如 `recall --recent`），请使用 PowerShell here-string（见上），或将多行内容先写入临时文件、由调用方读取后组装参数；不要依赖 CMD 引号内换行。

> **长路径提示**：Windows 默认 260 字符路径上限，`SKILL_ROOT` 较深 + 中文项目名可能超限；建议保持 Skill 目录层级简短，或启用系统"LongPathsEnabled"（注册表 `HKLM\SYSTEM\CurrentControlSet\Control\FileSystem\LongPathsEnabled=1`）。

### 5. 首次运行（标准迁移方式：目标平台自动下载模型）

```bash
python memory_cli.py init "<项目目录>" --name "<项目名>"
python memory_cli.py status "<项目目录>"
```

首次调用会下载 BAAI/bge-small-zh-v1.5，并在目标平台建立**自己的模型缓存**，此后可离线使用（详见第四节）。
- 如需手动预下载：`huggingface-cli download BAAI/bge-small-zh-v1.5`（新版命令为 `hf download`；不预下载也可，脚本首次运行自动拉取）
- **batch_update 首次部署前置**：`batch_update.py` 强制离线（`HF_HUB_OFFLINE=1` / `TRANSFORMERS_OFFLINE=1`），不负责新环境首次在线下载模型；新环境请先用上面的 `memory_cli.py init` 完成模型缓存建立，再使用 batch_update.py。

### 6. 验证安装

```bash
python memory_cli.py --help                              # 应列出 8 个子命令（init/node/search/recall/review/meta/schedule/status）
python memory_cli.py status "<项目目录>"                # 节点数 / 待生成向量（pending 应为 0）
python memory_cli.py search "<项目目录>" --query "测试" # 应返回结果或空列表，不报错
```

### 7. 已知坑

- **Windows 原生 Python 即可运行，无需 WSL**（脚本无 Linux 依赖；proot/Ubuntu 只是部署方式）
- 模型加载约 9s（缓存后）、首次约 43s（含下载）均属正常（见二.1）；脚本在已有本地缓存时自动/强制离线运行（memory_cli.py 自动检测缓存、batch_update.py 强制离线），避免加载时联网检查超时（实测未设离线时单次调用 >180 秒超时）
- **可选离线方案（目标平台完全无法联网时）**：准备一个可用的 Hugging Face 模型缓存（`huggingface-cli download` / `hf download` 预下载，或手动 export 导出的缓存均可），放入默认缓存目录（Linux `~/.cache/huggingface/hub/models--BAAI--bge-small-zh-v1.5/`，Windows `%USERPROFILE%\.cache\huggingface\hub\models--BAAI--bge-small-zh-v1.5\`）或 `HF_HOME`/`HF_HUB_CACHE` 指定位置，即可离线复用。**缓存须完整**：脚本离线检测会校验必要文件（任一 snapshot 含 config.json + modules.json + model.safetensors 或 pytorch_model.bin），仅目录存在/下载中断残留会被判为"未部署"并尝试联网（首次部署中断后重跑即可自动续传完成）。
- 模型更新时仅需改脚本内 `MODEL_NAME` 常量
