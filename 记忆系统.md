# study-coach · 记忆系统（知识树 + 温故知新）

> 本文件为记忆系统细节手册：CLI 完整参数（--source/--relations/--attachments）、批量写入、tree.json 结构、Focus Buffer、用户原句、非文本材料、检索预算等。SKILL.md §6 已有最小命令速查，需要完整参数或细节时阅读本文件。

## 记忆文件（项目文件夹内）
- **tree.json**：AI 专用知识树。只存：知识点名称、该生对该知识点的个性化摘要（用户自己的话归纳）、诊断记录（mastery 掌握度 / error_count 错误次数 / last_tested 最后检验日期 / last_learned 最后学习日期 / review_interval_days 复习间隔天数）、横向关联 relations（如"A 是 B 的前置"）。**禁止存入通用知识本体**（定义原文、公式推导、例题答案等），通用知识由 AI 自身能力或联网获取，不落盘。
- **_review.md**：人类专用温故知新文件（格式见《异常与附录.md》附录A）。**AI 检索记忆时默认忽略该文件**，不将其内容注入上下文；仅在用户要求"查看进度/复习记录"等明确展示场景下，读取其内容转述给用户（展示 ≠ 注入记忆）。

## 执行规则（强制）
部署/离线/模型缓存说明见《记忆系统-设计文档.md》§七，此处不重复。

## 记忆操作命令（CLI）
```bash
SKILL_ROOT="<你的 Skill 根目录>"
CLI="$SKILL_ROOT/scripts/memory_cli.py"   # 移植到其他平台时替换为目标环境实际 Skill 根目录或项目路径
PROJ="$SKILL_ROOT/<项目目录>"
# 初始化（幂等）：创建 tree.json + _review.md
python3 "$CLI" init "$PROJ" --name <项目名>
# 新增/更新知识点节点（name 已存在则更新；新节点默认挂 root；**用户实际学习的新知识点必须带 --learned**——自动记录 last_learned、初始化 review_due=明天并进入复习调度；不带 --learned 只创建课程蓝图/结构节点，不进入调度；**调度字段维护：不带 --learned 的同名更新/合并不重置 review_due 与 review_interval_days；带 --learned 的同名更新/转正已有 review_due 不覆盖（学习语义见"学习语义不变量"节）**；同名更新时 --parent 仅对新建生效，移动节点请用 node update；**summary 超过 1500 字会被 CLI 拒绝（硬上限），超过 1200 字输出 warning**）
python3 "$CLI" node add "$PROJ" --name <知识点名> --summary <用户个性化摘要> --parent <父节点名|id> [--learned] --mastery <0~1> --error_count <n> --last_tested <YYYY-MM-DD> [--source <user|ai|mixed>] [--relations '<JSON数组>'] [--clear_relations] [--attachments '<JSON数组>'] [--clear_attachments]
# 更新节点字段（mastery/error_count/last_tested/summary/relations/attachments 均可；--summary 为覆盖写入，复习摘要写回按 SKILL.md §5 第5步用 node add 拼接"旧文+新增"（不带 --learned）；--parent 可把节点移动到新父节点；**诊断字段（mastery/error_count/last_tested）只记录数据，不刷新 last_learned、不初始化复习调度**——前置诊断不能把 planned 节点推进成 learned；进入调度只由 node add --learned 或 schedule update 负责；纯内容修正同样不刷新）
python3 "$CLI" node update "$PROJ" --name <知识点名> [--parent <父节点名|id>] --mastery <0~1> --error_count <n> --last_tested <YYYY-MM-DD> [--source <user|ai|mixed>] [--relations '<JSON数组>'] [--clear_relations] [--attachments '<JSON数组>'] [--clear_attachments]
# 删除节点（连带删除其下所有子节点，谨慎使用）
python3 "$CLI" node rm "$PROJ" --name <知识点名>
# 按名精确读取节点（复习/写回前读旧摘要、对照基准用；不走相似度检索，不会漏召，返回 summary/诊断/relations/source/attachments/path）
python3 "$CLI" node get "$PROJ" --name <知识点名>
# 检索（HRS 分层剪枝，相似度阈值默认 0.45；--source 过滤来源，默认 all）
python3 "$CLI" search "$PROJ" --query <查询文本> [--source <user|ai|mixed|all>]
# 检索+预算合并（输出 context_block 注入回复上下文；预算默认 6000 token；--recent 传最近 6 轮，按时间正序，最后一行=最新一轮）
python3 "$CLI" recall "$PROJ" --query <查询文本> [--budget 6000] [--recent $'[用户精简] ...\n[AI精简] ...'] [--source <user|ai|mixed|all>]   # 注：$'...' 为 bash 多行字符串语法；PowerShell/CMD 下按各自语法传多行文本
# 温故知新追加（同日同小节重复调用会原位更新，未传字段保留原值）
python3 "$CLI" review "$PROJ" --date <YYYY-MM-DD> --section <小节主题> --what <学了什么> --insight <关键理解> --practice <练习/输出> --weak <薄弱点> --next <下一步>
# 项目元数据读写（项目级：目标/阶段/薄弱点/下一步行动等，存 tree.json 顶层 meta；写后自动同步 _review.md 概览）
python3 "$CLI" meta get "$PROJ" [--keys stage weak_points]
python3 "$CLI" meta set "$PROJ" --patch '{"stage": "阶段2/8", "next_actions": ["..."], "last_session_date": "<今天>"}'
# 复习调度：到期检查（--plan 输出紧凑复习计划，按单元分组+摘要截断，见 SKILL.md §5）/ 复习结果回写（更新 review_due；**仅限已正式学习节点的检验/复习结果回写——节点须已有 last_learned/review_due 等学习字段，planned 节点被 CLI 拒绝；单纯摸底/诊断请用 node update，不进入调度**）
python3 "$CLI" schedule due "$PROJ" [--plan]
python3 "$CLI" schedule update "$PROJ" --name <知识点名> --passed [--mastery <0~1>]
# 状态检查（节点数 / 待生成向量）
python3 "$CLI" status "$PROJ"
```

## 批量写入（推荐：一次会话收尾需执行多个写入操作时）
`scripts/batch_update.py` 在**单个进程内加载嵌入模型一次**，连续执行多个写入操作，结果与逐次调用 CLI 完全一致（原子写与向量降级逻辑不变）。脚本内部已强制离线模式。一次收尾（多个 node + meta + review + schedule_update）只需加载模型一次。
```bash
SKILL_ROOT="<你的 Skill 根目录>"
BATCH="$SKILL_ROOT/scripts/batch_update.py"   # 移植时替换为实际路径
PROJ="$SKILL_ROOT/<项目目录>"
# ops.json：JSON 数组，每项一个操作，如：
# [{"op":"node_update","name":"高光","summary":"…","mastery":0.9},
#  {"op":"meta_set","patch":{"stage":"阶段2/8","next_actions":["…"],"last_session_date":"2026-08-23"}},
#  {"op":"review","date":"2026-08-23","section":"第2课","what":"…","insight":"…"},
#  {"op":"schedule_update","name":"色相","passed":true,"mastery":0.9}]
python3 "$BATCH" "$PROJ" "<ops文件路径>"   # /tmp 为 Linux 示例；Windows 下用实际文件路径
```
> Windows 调用：上方为 Linux/bash 写法；Windows 下把 `BATCH`/`CLI` 换成实际路径、命令用 `python`（非 `python3`），PowerShell/CMD 完整示例见《记忆系统-设计文档.md》§七。
支持的操作：`init` / `node_add` / `node_update` / `node_rm` / `review` / `meta_set`（`patch` 或 `key`+`value`）/ `meta_get`（`keys` 数组，可选）/ `schedule_due` / `schedule_update`（`name`、`passed`、`mastery` 可选）/ `status`。node 操作字段与上方 CLI 参数一一对应（`node_add` 额外支持 `learned` 布尔字段，语义同 `--learned`）；任一操作失败不影响其余操作继续（结果含各操作 `ok` 标记）。
**部分失败恢复机制（无事务/rollback，靠检测 + 补写）**：
- 每次 batch 输出都会标注各操作 `ok`，AI 收尾时必须检查全部 `ok`；
- 有失败操作（`ok:false`）→ 立即重跑失败项（用 CLI 逐条重试，或生成仅含失败操作的 ops.json 再跑一次 batch）；
- 特别关注 `schedule_update` 失败（会导致"记录显示已学完但调度未更新"）与 `node_update` 失败（节点数据未落盘）——重试后即可恢复；
- 若本次会话无法重试，把失败操作进入可靠待办状态（如 meta.notes："batch 补写：<操作描述>"），下次会话开始时先补写再继续；**不允许静默丢弃失败项**。

## tree.json 文件结构（AI 直接读文件时）
> 顶层键：`schema_version` / `project` / `updated` / `root` / `meta`；向量生成失败的节点 id 会登记在顶层 `pending_vectors`（检索时空向量节点**跳过自身但继续深入其子层**——防止中间节点向量失败导致整棵子树不可召回；下次写入自动重试，详见《记忆系统-设计文档.md》三.5）。**所有知识节点挂在 `root.children` 下**（root 自身 id="root"，不是知识点；遍历节点树先取 `data["root"]`，勿假设顶层即节点）；`meta` 为项目级元数据（goal/stage/weak_points/notes 等）。常规读写一律走 CLI（node add/update/recall、meta get/set）；直接读文件仅限检查、批量补录、数据修复。

## 学习语义不变量（唯一核心规则）

**诊断 ≠ 学习；测试 ≠ 学习；普通数据修改 ≠ 学习。**

只有以下两个入口会产生正式学习/复习语义（刷新 last_learned、追加 learning_history、推进/刷新复习调度）：
1. `node add --learned`（显式学习/转正，含同名更新与合并）；
2. `schedule update`（复习/检验结果回写，通过/失败均刷新）。

其余写入均**不产生**学习语义：不带 --learned 的 node add（课程蓝图/结构写入、复习摘要写回）、node update（含带诊断字段的前置诊断/数据记录）、纯内容修正（仅改 summary/relations/attachments/source 或移动节点）——前置诊断只写 mastery/error_count/last_tested，不改变节点生命周期（节点存在 ≠ 用户学过；被诊断/测试 ≠ 已学习）。

各文件引用本不变量，不重复展开整段生命周期解释：SKILL.md §5 存储字段/节点生命周期、记忆系统-设计文档.md §三.10、异常与附录.md schedule update 拒绝条目、scripts/memory_cli.py 与 batch_update.py 注释。

## 项目目录命名规则（用户可见项目名 ≠ 文件系统目录名）

- **用户可见名与目录名分离**：tree.json 顶层 `project` 存用户可见项目名（init --name 原样传入，可含任意字符，如 "C++/Windows 学习"）；目录名由 `init` 自动安全化（确定性映射，不引入 UUID）。
- **"/"的调用方安全规则**：用户可见项目名含 `/` 时，不得直接将其作为 project_dir 的路径层级传给 CLI——调用方需先生成安全目录名（如把 `/` 替换为 `-`），其余字符安全化由 init 兜底。
- **使用 init 返回的实际目录**：`init` 新建项目（tree.json 不存在）时在父目录下创建安全目录，输出返回实际 `project_dir`，后续命令一律以返回路径为准；旧项目已存在（project 一致或未传 `--name`）保持幂等复用，不破坏旧数据；撞名时绝不静默复用/误合并（防冲突与映射稳定性细节由 CLI 实现保证）。

## 记忆写入时机
| 时机 | 操作 |
|---|---|
| 技能一规划确认后 | init（幂等）＋ 为各阶段/目标创建节点（--parent root，**显式 --source ai**——蓝图/阶段结构由 AI 生成，非用户理解；**不带 --learned**——课程蓝图节点不进入复习调度，用户真正学习时才标记） |
| 技能二每小节教学后 | 本节点知识点 node add（**用户实际学习的新知识点带 --learned**）或 node update（--parent 按课程结构显式指定）；review 追加"学了什么/关键理解"；meta 更新（含 last_session_date） |
| 技能三检验后 | **先 `node get` 检查节点状态（SKILL.md §4 第0.5步）**：已正式学习（有学习字段）→ 被检验知识点 node update（更新 mastery/error_count/last_tested，仅记录数据）；通过检验 → schedule update --passed，未通过 → 按 §5 失败状态机执行（不带 --passed，必须带 --mastery 0.5/0.3，失败重置间隔）；**last_learned/learning_history/review_due 由 schedule update 统一维护（planned 节点会被 CLI 拒绝——未正式学习只可摸底，不得调用 schedule update）**；review 追加"练习输出/薄弱点/下一步"。若未进技能三，技能二结束时一次写全 |
| 小节收尾（技能二/三结束后） | schedule due 检查到期复习 → 有到期先走复习流程（见 SKILL.md §5）→ 复习结果按 SKILL.md §5 第4~6步回写 |
| "继续学X"恢复时 | schedule due 检查到期复习（有则先复习）；recall 检索薄弱点与最近检验结果 |
| 每次对话结束前 | 检查本轮记忆是否已按上述时机更新完毕；未更新则补写，确保记忆完整落盘；并按 Focus Buffer 节覆盖写入 _session_context.md（最近 6 轮） |

**非文本与用户原句落盘**：收到非文本材料时按"非文本材料"节处理、用户原话命中"用户原句机制"三问时按该节摘录进节点 summary，均与对应节点写入同批完成落盘。

## 节点挂载规则
新知识点默认挂 root；**子节点挂载必须显式 --parent 指定**（依据课程/教材结构，如"色相"→"色彩三要素"）。同名节点重复 add 自动更新而非重复创建；相似度 ≥0.85（当前默认值，可调整；依据见《记忆系统-设计文档.md》三.4）的重复知识点自动合并（防重复节点；**仅未显式 --parent 时触发**——显式指定父节点视为用户明确意图，不做自动合并，AI 需自行避免语义重复写入）。
**挂载结构约定（防挂乱）**：按课程/教材蓝图挂载——每节课的核心知识点挂 root 或挂到课程章节节点；**禁止把多节课的内容全部挂到某一节课的节点下**（如"直方图"节点下只挂直方图相关子知识点，不能挂"曲线/HSL精修/风格化"等后续课程）。规划阶段即确定蓝图（写入 meta.notes），挂载时对照蓝图执行。

## Focus Buffer（短期记忆，跨会话）
短期焦点缓冲落盘为项目文件夹内 `_session_context.md`（**由 AI 直接读写文件，不经过 CLI**），只保存**最近一次会话最后 6 轮**（1 轮 = 用户输入 + AI 回复）。
> 以下数值（6 轮 / 300 字 / 150 字 / 6000 token）均为当前默认值，可调整。
**写入**：每次会话结束前，AI 提取当前会话有效的最后 6 轮，精简后**覆盖写入** `_session_context.md`，六轮以前内容一律删除。每行直接以 `[` 开头（**不带任何列表符号/Markdown 标记**；若误带 `- ` 等前缀，CLI 会宽容解析，但规范要求不带），格式：
- `[用户原句]：...` — 用户原话，仅用于需精确引用的表达（≤50 字，原文不改字）；无则省略该行
- `[用户精简]：...` — AI 对用户输入的精简（≤300 字）
- `[AI精简]：...` — AI 对自己回复的精简（≤150 字）
- 省略规则：只有原句无精简则只写原句；不写占位行
**读取与合并（"继续学X"或技能三第 0 步）**：
- 当前会话轮次 ≥6：只用当前最近 6 轮，忽略历史
- 当前会话轮次 <6：用当前全部轮次，再从 _session_context.md（上次会话最后 6 轮）末尾补足到 6 轮，按时间正序排列
- 合并结果传入 `recall --recent`
**recall --recent 格式**：每行一条，CLI 识别 `[用户原句]`/`[用户精简]`/`[AI精简]` 前缀；兼容旧格式 `user:`/`assistant:`（映射为 用户精简/AI 精简）。用户原句行 CLI 不截断、不改字；用户精简/AI 精简兜底 300/150 字截断。按时间正序传入，最后一行必须是最近一轮。
**预算**：总 6000 token，超限先淘汰旧轮次、不截断任何一条；最近 1 轮（最后 [AI精简] 行 + 其同轮的全部 [用户原句]/[用户精简] 行）强制保留（CLI 内部以 user_quote/user_simplified 标识）。

## 用户原句机制（用户原话摘录，原"金句机制"）
**判断标准（三问，命中 ≥2 即用户原句）**：
1. **惊奇感**：反直觉、打破常规、让 AI 没想到（例："调色最重要的不是色准，是情绪"）
2. **普适性**：换个人、换个时间依然成立，排除当下流水账（"我昨天拍了三组照片"✗）
3. **凝练感**：短小、完整、像一句断言而非过程叙述（例："后期不是修图，是还原本来的感觉"）
- **期望锚点（经验值）**：一节课用户原句 0~3 句为正常范围，长期偏离说明判断有偏向；与 goals 相关的表达（动机/目标/自我要求）优先
- **存储**：内嵌在节点 summary 文本中，用『』引号标注，≤50 字，摘录原文不改字；多句并列 `『A』『B』`；与来源段落标记【用户】【AI】不冲突，可并存（如 `『原句』【用户】回讲补充…`）
- **引用规则**：引号内可原样引用（"你曾经说『……』"）；非引号内容只能概括引用（"你上次提到……"），**不能说成原话**
- **使用时机**：用户原句只在两种场合使用——①用户想不起来时作提示抛出（配合 SKILL.md §5 渐进式追问的摘要展示）；②学习/检验/复习收尾时作总结加深印象。其他时候不主动抛出；用户原句不作考题（出题一律从知识点本身，见 SKILL.md §5 第3步）。

## 非文本材料
- **处理边界**：仅当非文本材料**与当前学习相关**时才进入本流程
- **按需保留**：AI 根据后续学习是否需要参考原文件中的非文字信息（如图表、版式、原始文件本身）决定是否将原文件复制到 attachments/ 并记录引用；若仅需提取文字，且文字内容已写入节点摘要，则不必保留原文件
- **语音**：由平台语音转文字能力（ASR）转成文字进树——进入知识树的是文字化、个性化的知识，而不是音频原文；原音频不存
- **纯文本笔记**（用户提供的个性化内容）：由 AI 提取要点进节点 summary（仍遵守"不存通用知识本体"）；原始文件按需复制进 attachments/
- **非文本文件**：AI 识图/读取 → 文字描述进 summary（例："用户展示了暗部提亮对比图，高光控制有问题"）；**正文内容不进树，只存描述 + 路径引用**（正文属通用知识本体，不得进 tree.json；教材按"教材拆分"节处理）
- **隔离文件夹**：收到非文本材料时，若按需保留原则决定保留，**AI 调用平台文件工具把原文件复制到** `<项目目录>/attachments/`（不动原文件，原地址被删不影响），AI 后续只认隔离目录里的这份；CLI 不负责文件复制，只存引用
- **教材拆分（讲义性质）**：用户提供教材且个性化教学大纲生成后，按大纲单元拆分教材内容生成 `.md` 文件，存于 `<项目目录>/attachments/materials/`（按需创建，无教材不创建）。拆分时机：规划确认时教材已到手则立即拆；否则推迟到技能二模式A（用户提供材料）时再拆。命名 `materials/<教材名>-<大纲单元名>.md`；文件含原文摘录 + 单元级详细解析、易错点、练习 + AI/联网补充内容（与原文分区标注，来源需区分）。**原始教材文件保留在 attachments/ 根目录不删除**（拆分文件是快速访问摘录，非唯一副本）。相关节点通过 `--attachments` 字段记录路径+描述（如 `attachments/materials/调色教材-色彩三要素.md | 色彩三要素拆分讲义`）；节点 summary 不重复材料详细内容
- **命名规则**：`{节点名}-{用户描述}.{扩展名}`（例：`曲线-暗部提亮前后对比图.jpg`）；描述由 AI 概括但**保留用户关键用词**（"暗部提亮""高光溢出"等特征词必须一致，未来按名匹配）；≤30 字；节点名含文件系统非法字符时替换为 `-`；同节点多图描述必须可区分，撞名加序号（以上规则由 AI 自觉执行，CLI 不校验）
- **attachments 字段**（节点级）：存隔离目录路径 + 描述（例：`attachments/曲线-暗部提亮前后对比图.jpg | 暗部提亮前后对比，高光控制有问题`），经 `node add/update --attachments '<JSON数组>'` 写入，去重追加；原路径不存（会被删）；**更新/补全某条描述需先 `--clear_attachments` 再整体传入**（无单条覆盖）
- **节点来源 source 字段（tree.json 节点级，区分记忆来源）**：
  - `user`：用户自己的理解/原句/个性化摘要（默认值）
  - `ai`：AI 教学记忆、材料解析、章节结构、讲解思路、易错点、教学进度等
  - `mixed`：同一节点同时含用户理解与 AI 解析（如回讲后补充用户理解）；summary 内用【用户】/【AI】前缀区分段落
  - 写入：`node add/update --source <值>`，**仅显式传入时更新**；新建默认 user；旧节点无字段视为 user。AI 对教材解析可挂教材名节点下（source=ai），挂载规则同课程蓝图约定。更新 mixed 节点：先读原 summary，手动拼接新内容后整体传入 --summary，并设 --source mixed
  - **状态转移规则（source 必须如实反映内容构成，不允许滞后）**：
    - user + AI 内容（AI 讲解/解析补充进节点）→ **mixed**
    - ai + 用户内容（回讲补充、复习新理解/原句写回节点）→ **mixed**
    - mixed + 任何一方继续补充 → 保持 **mixed**
    - 触发方式：产生混合内容的那次写入必须显式传 `--source mixed`；复习摘要写回（SKILL.md §5 第5步）若写入了用户新理解/原句，原节点非 mixed 时必须同时传 `--source mixed`；skill 二教学把 AI 解析写入已有 user 节点时同理
  - tree.json 节点 summary 只存知识点级精炼要点（目标"一句话级"，通常数十字至一两百字；长度限制见 CLI 节），不存单元级详细解析（单元级解析在拆分讲义文件）
- **匹配逻辑**：用户提到某非文本材料特征 → 按特征词匹配文件名 → 命中后定向读取该附件重新识别；描述缺失或不够 → 重新定向识别补全；匹配不到 → 问用户或重看目录

## 记忆错误判断（用户质疑记忆时）
用户说"你记错了吧/我记得是……"时，AI 先自检 summary 记的点：
- **确认自己记的点正确** → 判断结论随意（坚持用户记错或让步都行）——记忆正确，不影响后续
- **发现自己记的点错误** → **必须修改 summary**，不得坚持；修正后向用户确认
- **无法自检** → 以用户为准修正（坚持错误记忆的代价 > 修正正确记忆的代价）；用户表述也含糊则标记待下次验证
核心：**谁对谁错可以含糊，但记忆里不能留已知的错误**。

## 检索与预算
- 相似度阈值默认 0.45（当前默认值，可调整；依据与调整方法见《记忆系统-设计文档.md》三.1）：低于阈值的子树剪掉；动态阈值上浮、内部访问上限、旁路直查等内部机制见《记忆系统-设计文档.md》三.1，此处不重复
- 注入预算默认值与淘汰规则同 Focus Buffer 节：树检索结果与焦点缓冲统一按 Utility/Token 贪心装入
- recall 输出的 context_block 作为"记忆上下文"注入本轮回复，与当前对话内容并列使用
- **source / learning_history / first_learned 只出现在 search / recall 的完整输出（hits）中，不放入 context_block**，避免挤占预算；AI 需要时直接用本次结果，或重新调用 search / recall 查询该节点；**禁止读取 tree.json 文件获取这些字段**
- **精确访问 vs 语义检索优先级（精确意图 > 语义检索；访问上限/剪枝只约束开放式检索）**：
  - 必须 `node get --name`（精确匹配，不走 HRS，不会漏召）：用户明确点名某知识点（"复习色相""讲讲白平衡"）、复习 due 项、复习/写回前读旧摘要、用户质疑记忆（"你是不是记错了"→ 先 node get 自检该节点）
  - 用 `recall` / `search`（语义检索）：开放式问题（"我最近学过哪些和颜色有关的内容？"）、回忆薄弱点、注入上下文基准
  - 禁止用 search/recall 的剪枝结果替代 node get 回答点名问题——剪枝/访问上限可能漏召，点名问题必须精确访问

## 学习历史（learning_history / first_learned，按需使用）
- `learning_history`：节点级，字符串数组，按时间正序保存最近 10 次学习/复习日期（YYYY-MM-DD），超 10 条自动移除最早；`first_learned`：最早学习日期，首次学习设置后不变
- 维护规则：node add **带 --learned 的真正新建**（added）时追加今天并设 first_learned；node add --learned 同名更新/合并（显式学习/转正）追加今天、first_learned 为空则设为今天；schedule update（复习通过/失败）追加今天、first_learned 为空则设为今天；不带 --learned 的 node add、node update 普通修改（summary/relations/attachments 等）**不追加**。同日多次学习同节点只保留一个日期
- **last_learned 触发条件（与 learning_history 同源但独立）**：node add --learned（含同名更新/合并，显式学习语义）或 schedule update（复习/检验结果回写，通过/失败均刷新）时置为今天；不带 --learned 的 node add（课程蓝图/结构写入、复习摘要写回）、node update（**含带诊断字段的前置诊断/数据记录**）与纯内容修正（仅改 summary/relations/attachments/source 或移动节点）不刷新——last_learned 只代表真实学习/复习/检验行为，不代表任意数据修改（学习语义见"学习语义不变量"节）
- 这两个字段**不参与复习调度**（仍用 `review_due`）和检索 Utility 计算，仅供 AI 按需引用（如"你去年冬天学……的时候"）
